{
  "userStories": [
    {
      "id": "US-001",
      "title": "Add BLOCKED story status and rich Telegram notification to prd_manager.py and ralph.py",
      "description": "Files: ralph/prd_manager.py and ralph/ralph.py\n\nProblem: When a story hits max_attempts, Ralph sends a generic Telegram notification ('blocked \u2014 manual intervention needed') with no detail about which story failed or why. This means Jill has to SSH and grep logs to understand what happened.\n\nFix Part 1 \u2014 prd_manager.py:\nAdd a new function mark_story_blocked() that sets status='blocked' on a story when it hits max_attempts:\n\n```python\ndef mark_story_blocked(prd: dict, story_id: str, error: str = \"\") -> dict:\n    \"\"\"Mark a story as permanently blocked (hit max attempts). Returns updated prd.\"\"\"\n    for story in prd.get(\"userStories\", []):\n        if story[\"id\"] == story_id:\n            story[\"status\"] = \"blocked\"\n            story[\"blockedAt\"] = datetime.now(timezone.utc).isoformat()\n            story[\"blockReason\"] = error[:500] if error else \"max attempts reached\"\n            log.warning(f\"Story {story_id} marked BLOCKED: {error[:80]}\")\n            break\n    return prd\n```\n\nAlso add a helper to get all blocked stories:\n```python\ndef get_blocked_stories(prd: dict, max_attempts: int = 3) -> list:\n    \"\"\"Return list of stories that have hit max_attempts without passing.\"\"\"\n    return [\n        s for s in prd.get(\"userStories\", [])\n        if not s.get(\"passes\", False) and s.get(\"attempts\", 0) >= max_attempts\n    ]\n```\n\nFix Part 2 \u2014 ralph.py:\nUpdate the imports at the top to include the new functions:\n```python\nfrom prd_manager import (\n    load_prd, save_prd, get_next_story, all_done, any_blocked,\n    mark_story_done, mark_story_failed, mark_story_blocked, get_blocked_stories,\n    append_progress, story_summary\n)\n```\n\nUpdate the blocked notification at the bottom of main() (currently around line 800):\n\nCurrent:\n```python\nelif any_blocked(prd, cfg.get(\"max_attempts_per_story\", 3)):\n    log.warning(\"Some stories have hit max attempts \u2014 manual intervention needed.\")\n    notify(f\"\u26a0\ufe0f '{slug}' blocked \u2014 manual intervention needed\", log)\n```\n\nReplace with:\n```python\nelif any_blocked(prd, cfg.get(\"max_attempts_per_story\", 3)):\n    max_att = cfg.get(\"max_attempts_per_story\", 3)\n    blocked = get_blocked_stories(prd, max_att)\n    for b_story in blocked:\n        mark_story_blocked(prd, b_story[\"id\"], b_story.get(\"error\", \"max attempts reached\"))\n    save_prd(slug, prd)\n    # Build rich notification with story ID + last error for each blocked story\n    lines = [f\"\u26d4 Ralph blocked in '{slug}' \u2014 manual intervention needed\"]\n    for b_story in blocked:\n        sid = b_story[\"id\"]\n        reason = b_story.get(\"error\", \"unknown\")[:120]\n        lines.append(f\"  \u2022 {sid}: {reason}\")\n    notify(\"\\n\".join(lines), log)\n    log.warning(\"\\n\".join(lines))\n```",
      "acceptanceCriteria": [
        "python3 -m py_compile ralph/prd_manager.py",
        "python3 -m py_compile ralph/ralph.py",
        "grep -q 'mark_story_blocked' ralph/prd_manager.py",
        "grep -q 'get_blocked_stories' ralph/prd_manager.py",
        "grep -q 'mark_story_blocked' ralph/ralph.py",
        "python3 -c \"import sys; sys.path.insert(0,'ralph'); from prd_manager import mark_story_blocked, get_blocked_stories; prd={'userStories':[{'id':'US-001','attempts':3,'passes':False,'error':'test error'}]}; prd=mark_story_blocked(prd,'US-001','test error'); s=prd['userStories'][0]; assert s.get('status')=='blocked', f'Expected blocked, got {s}'; assert 'blockedAt' in s; print('BLOCKED STATUS PASS')\""
      ],
      "qualityChecks": [
        "python3 -m py_compile ralph/prd_manager.py",
        "python3 -m py_compile ralph/ralph.py"
      ],
      "priority": 2,
      "passes": true,
      "attempts": 0,
      "dependsOn": [],
      "contextFiles": [
        "ralph/prd_manager.py",
        "ralph/ralph.py"
      ],
      "_slug": "ralph-blocked-status",
      "notes": "Implemented US-001: Added BLOCKED story status and rich Telegram notifications.\n\nChanges:\n1. prd_manager.py: Added `mark_story_blocked()` to set status='blocked' with timestamp and reason, and `get_bl"
    }
  ]
}