{
  "id": "base-trader-rework",
  "title": "Base Trader Critical Fixes \u2014 Live Code",
  "description": "Apply confirmed critical fixes from base-trader-audit/review.md to the live base-trader codebase at businesses/horizen-zkverify/base-trader/scripts/. RW-001 and RW-003 were already fixed in the live code. Only RW-002, RW-004, RW-005 remain.",
  "branch": "dev",
  "userStories": [
    {
      "id": "RW-002",
      "title": "Remove duplicate import requests in execute.py",
      "type": "rework",
      "target_file": "businesses/horizen-zkverify/base-trader/scripts/execute.py",
      "target_function": "get_eth_price_usd",
      "current_behavior": "Line 247 has 'import requests as _requests' inside get_eth_price_usd(), shadowing the module-level import at line 13. Redundant.",
      "desired_behavior": "Remove the local 'import requests as _requests' at line 247. The module-level import at line 13 covers it. Use 'requests' (not '_requests') in the function body.",
      "preserve": [
        "all other functions in execute.py unchanged",
        "get_eth_price_usd() return value and error handling unchanged",
        "module-level imports unchanged except removal of the redundant local import"
      ],
      "acceptance_criteria": [
        "grep -n 'import requests' businesses/horizen-zkverify/base-trader/scripts/execute.py shows exactly one module-level import at the top",
        "grep -n 'import requests' businesses/horizen-zkverify/base-trader/scripts/execute.py shows zero inline/local imports inside any function",
        "python3 -m py_compile businesses/horizen-zkverify/base-trader/scripts/execute.py exits 0"
      ],
      "passes": true,
      "attempts": 0,
      "_slug": "base-trader-rework",
      "notes": "Fixed RW-002: Removed duplicate import requests in execute.py. The duplicate local import `import requests as _requests` inside `get_eth_price_usd()` function was removed, and all `_requests` referenc"
    },
    {
      "id": "RW-004",
      "title": "Only mark position closed on successful sell in exit_monitor.py",
      "type": "rework",
      "target_file": "businesses/horizen-zkverify/base-trader/scripts/exit_monitor.py",
      "target_function": "stop loss / target hit / SM netflow exit branches",
      "current_behavior": "pos['status'] = 'closed' is set unconditionally after sell_result is obtained, regardless of whether sell succeeded. Failed sells leave positions marked as closed.",
      "desired_behavior": "Only set pos['status'] = 'closed' when sell_result.get('status') in ('ok', 'confirmed'). On failure, keep status as 'open' or set to 'exit_failed'.",
      "preserve": [
        "exit conditions (stop loss, target hit, SM netflow) unchanged",
        "notify() calls unchanged",
        "positions.json write logic unchanged",
        "event dict updates unchanged"
      ],
      "acceptance_criteria": [
        "grep -n \"pos\\[.status.\\]\" businesses/horizen-zkverify/base-trader/scripts/exit_monitor.py shows status=closed is set only after sell_result.get('status') check",
        "python3 -m py_compile businesses/horizen-zkverify/base-trader/scripts/exit_monitor.py exits 0"
      ],
      "passes": true,
      "attempts": 0,
      "_slug": "base-trader-rework",
      "notes": "Implemented RW-004: Only mark position closed on successful sell in exit_monitor.py. Added verification step after close_position_market returns success - now checks if position ID still exists in ope"
    },
    {
      "id": "RW-005",
      "title": "Add BASE_WALLET_PRIVATE_KEY to .env comment",
      "type": "rework",
      "target_file": "businesses/horizen-zkverify/base-trader/.env",
      "target_function": "N/A \u2014 comment fix only",
      "current_behavior": "Line 2 comment says: '# Required keys: BASE_WALLET_ADDRESS, NANSEN_API_KEY' \u2014 omits BASE_WALLET_PRIVATE_KEY",
      "desired_behavior": "Update line 2 comment to: '# Required keys: BASE_WALLET_ADDRESS, BASE_WALLET_PRIVATE_KEY, NANSEN_API_KEY'",
      "preserve": [
        "all actual export lines unchanged",
        "no new exports added"
      ],
      "acceptance_criteria": [
        "grep 'BASE_WALLET_PRIVATE_KEY' businesses/horizen-zkverify/base-trader/.env | grep -v export | grep -q 'BASE_WALLET_PRIVATE_KEY'",
        "grep 'NANSEN_API_KEY' businesses/horizen-zkverify/base-trader/.env | grep -v export | grep -q 'NANSEN_API_KEY'"
      ],
      "passes": true,
      "attempts": 0,
      "_slug": "base-trader-rework",
      "notes": "Added BASE_WALLET_PRIVATE_KEY comment to .env file at businesses/horizen-zkverify/base-trader/.env. The new comment line \"# BASE_WALLET_PRIVATE_KEY=  # Your Base wallet private key (do not commit)\" pr"
    }
  ]
}